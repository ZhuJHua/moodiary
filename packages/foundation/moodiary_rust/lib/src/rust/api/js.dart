// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';

import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

/// 跑一段 JavaScript，返回最后一个表达式的值。
///
/// **上限不过桥，全在 Rust 侧钉死**（见 [`JsLimits::default`]）。让 Dart 传进来
/// 就等于让每个调用点都有机会传出一个没有上限的沙箱，而那种缺陷在界面上没有症状。
///
/// 同步执行、跑完即弃：QuickJS 的 runtime 不是 `Send`，它在这一次调用的工作线程上
/// 生死，不跨 `await`、不复用。
Future<JsOutcome> jsEval({required String code}) =>
    RustLib.instance.api.crateApiJsJsEval(code: code);

class JsOutcome {
  /// 最后一个表达式的值。对象是 `JSON.stringify` 后的文本，`undefined` 为空串。
  final String value;

  /// `console.log` / `info` / `warn` / `error` 的行，带 `[LOG]` 一类前缀。
  final List<String> logs;

  /// 结果或日志超了上限、被截断过。
  final bool truncated;

  const JsOutcome({
    required this.value,
    required this.logs,
    required this.truncated,
  });

  @override
  int get hashCode => value.hashCode ^ logs.hashCode ^ truncated.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is JsOutcome &&
          runtimeType == other.runtimeType &&
          value == other.value &&
          logs == other.logs &&
          truncated == other.truncated;
}
