// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.13.0-beta.5.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

// These functions are ignored because they are not marked as `pub`: `anchor_of`, `base_docx`, `block`, `blocks`, `content_width_emu`, `fonts`, `half_pt`, `image_run`, `indented`, `inline_colored`, `inline`, `link_paragraph`, `list`, `meta_line`, `numbering_def`, `quoted`, `run_fonts`, `styled_run`, `table`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `Ctx`

/// 把一批日记写成一个 .docx 文件。
///
/// [docs_json] 是 `ExportDoc.toJson()` 的**数组**。每篇一文件的场景由 Dart 侧循环调用、
/// 每次传单元素数组实现 —— 合并与拆分共用同一条码路。
///
/// 直接落盘而不回传字节：与 `Zip::new(file_path)`、`ImageCompressor::optimize_to_file`
/// 的既有约定一致，且带图日记几十 MB 时不必在内存里整份成型。
Future<void> writeDocx({
  required String docsJson,
  required DocxStyle style,
  required String outPath,
}) => RustLib.instance.api.crateApiDocxWriteDocx(
  docsJson: docsJson,
  style: style,
  outPath: outPath,
);

/// 排版配置。字体只写名字，不嵌文件。
class DocxStyle {
  /// 中文字体名（写进 `w:rFonts` 的 `eastAsia`）。
  final String eastAsiaFont;

  /// 西文字体名（`ascii` / `hAnsi`）。
  final String asciiFont;

  /// 正文字号（磅）。
  final double fontSizePt;

  /// 行距倍数（1.0 = 单倍）。
  final double lineSpacing;

  /// 正文首行缩进两字符（中文排版习惯）。
  final bool firstLineIndent;

  /// 页面宽高（twip，1/1440 英寸）。A4 = 11906 × 16838。
  final int pageWidth;
  final int pageHeight;

  /// 四边页边距（twip）。
  final int pageMargin;

  /// 每篇日记前写标题。
  final bool includeTitle;

  /// 标题下写一行「日期 · 天气 · 位置 · 分类」摘要。
  final bool includeMeta;

  /// 多篇合并时每篇之间插分页符。
  final bool pageBreakBetween;

  /// 音视频占位行的类型词（已本地化，由 Dart 传入 —— 这一侧没有 l10n）。
  final String videoLabel;
  final String audioLabel;

  const DocxStyle({
    required this.eastAsiaFont,
    required this.asciiFont,
    required this.fontSizePt,
    required this.lineSpacing,
    required this.firstLineIndent,
    required this.pageWidth,
    required this.pageHeight,
    required this.pageMargin,
    required this.includeTitle,
    required this.includeMeta,
    required this.pageBreakBetween,
    required this.videoLabel,
    required this.audioLabel,
  });

  @override
  int get hashCode =>
      eastAsiaFont.hashCode ^
      asciiFont.hashCode ^
      fontSizePt.hashCode ^
      lineSpacing.hashCode ^
      firstLineIndent.hashCode ^
      pageWidth.hashCode ^
      pageHeight.hashCode ^
      pageMargin.hashCode ^
      includeTitle.hashCode ^
      includeMeta.hashCode ^
      pageBreakBetween.hashCode ^
      videoLabel.hashCode ^
      audioLabel.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is DocxStyle &&
          runtimeType == other.runtimeType &&
          eastAsiaFont == other.eastAsiaFont &&
          asciiFont == other.asciiFont &&
          fontSizePt == other.fontSizePt &&
          lineSpacing == other.lineSpacing &&
          firstLineIndent == other.firstLineIndent &&
          pageWidth == other.pageWidth &&
          pageHeight == other.pageHeight &&
          pageMargin == other.pageMargin &&
          includeTitle == other.includeTitle &&
          includeMeta == other.includeMeta &&
          pageBreakBetween == other.pageBreakBetween &&
          videoLabel == other.videoLabel &&
          audioLabel == other.audioLabel;
}
