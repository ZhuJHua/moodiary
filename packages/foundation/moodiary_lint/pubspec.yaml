name: moodiary_lint
description: "Shared analyzer and lint options for the workspace."
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.13.0"

dependencies:
  flutter_lints: 6.0.0
