name: moodiary_sqlite_vec
description: "sqlite-vec 0.1.9 built from source as a code asset and registered with the host SQLite."
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.13.0"
  flutter: ">=3.47.0"

dependencies:
  code_assets: 1.2.1
  ffi: 2.2.0
  hooks: 2.2.0
  logging: 1.3.0
  native_toolchain_c: 0.19.3
  sqlite3: 3.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  moodiary_lint: 0.0.1
