name: moodiary_platform
description: "平台服务：应用目录、缓存目录、生物识别能力探测。"
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.13.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter

  moodiary_utils: 0.0.1
  path_provider: 2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  moodiary_lint: 0.0.1
